# OAuth Tokens Folder

This project stores Google OAuth tokens in the `tokens/` directory (ignored by git).

To initialize locally:
1. Run the app once with valid OAuth client credentials.
2. Complete the browser login flow.
3. The tokens will be saved under `tokens/` automatically.

# Data Folder

The application uses the `data/` directory for the H2 database and other runtime files.
This folder is ignored by git to avoid committing local state.

If you need a clean start, delete the folder and the app will recreate it.
